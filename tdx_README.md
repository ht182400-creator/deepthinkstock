# 通达信周线指标 · 使用说明（质量+Regime / 动量+Regime）

两个 `.txt` 文件是**通达信技术指标公式**，用于在个股【周线】图上叠加本策略的
买卖点 / 空仓点。它们是回测引擎（`backtest_weekly.py`）中**逐股信号逻辑**的
平移，方便你肉眼对照与手动执行。

| 文件 | 对应策略 | 买点颜色 |
|------|----------|----------|
| `tdx_质量_regime.txt`  | 周频 质量+Regime（防御） | 蓝线 MA52 |
| `tdx_动量_regime.txt`  | 周频 动量+Regime（高弹性） | 橙线 MA52 |

---

## 1. 怎么装

1. 打开通达信 → 按 `Ctrl+F` → 公式管理器 → **技术指标公式** → 新建。
2. 公式名称自取（如 `ZL_QUALITY_REGIME` / `ZL_MOMENTUM_REGIME`）。
3. 把对应 `.txt` **全文**粘贴进编辑框 → 确定。
4. 在任意个股 K 线图切到 **周线** 周期，输入公式名调用，叠加在主图。

---

## 2. 指标画了什么

- **蓝/橙线 `MA52`**：52 周均线，即趋势线。现价站上它才考虑买。
- **红箭头（图标 8）**：买点。条件 = `上证>56周MA`（大盘多头）∧ `现价>52周MA` ∧ `52周动量>0`，
  且仅在本周**新触发**时画（已持仓不重复画）。
- **绿箭头（图标 9）**：卖点 / 退出。条件 = `破52周MA` ∨ `52周动量≤-40%` ∨ `估值分位≈顶部` ∨ `大盘翻空`。
- **淡红背景 + “空仓”文字**：当 `上证 ≤ 56周MA`（Regime 空头）时整图淡红并标注“空仓”——
  此时框架纪律是**全部持币，不新建仓**。
- **小三角（图标 3，位于最低价下方）**：量能蓄势标记（OBV 站上 26 周蓄势线 **且** 周量 > 26 周均量）。
  **仅作可视化参考，不进入买点条件**（见第 4 节原因）。

---

## 3. 通达信能复现 / 不能复现什么（重要）

| 能复现（本指标） | 不能复现（需外部清单） |
|------------------|------------------------|
| 单股的 52 周趋势/动量 | **横截面排名**：每周从全市场挑前 N 名 |
| 上证 56 周 Regime 多空 | **基本面质量门**（ROE≥10% & 正经营现金流） |
| 量能蓄势可视化 | 资金约束（3 万/4 仓、≤75 元整手） |

> 因此：本指标**不是自动选股器**。正确用法是 ——
> 先用外部候选池清单（见 `buy_list.html` / `buy_list_momentum.html`）锁定标的，
> 再切到周线看本指标，确认 **Regime 多头 + 出现红箭头** 时执行；**背景淡红（空仓）时一律不动**。

---

## 4. 为什么量能只做“参考”而不做“买点门槛”

按你的建议，我把“量在价先 / 主力资金进场”做成了 OBV 蓄势 + 放量因子，并回测对照（2018–2026，周频）：

| 周频配置 | 年化 | 最大回撤 | 夏普 | 候选池 |
|---|---|---|---|---|
| 质量+Regime（基线·无量能） | **5.9%** | −38.2% | **0.20** | 67 |
| 质量+Regime 量能门槛 | 3.4% | −41.6% | 0.07 | 25 |
| 动量+Regime（基线·无量能） | **12.6%** | −43.2% | **0.40** | 278 |
| 动量+Regime 量能门槛 | 7.6% | −42.4% | 0.21 | 107 |

**结论：量能因子在此框架上反而更差**——所有量能变体的年化/夏普都低于无量和基线。
原因：① OBV 本质≈价格的带符号累计代理，与动量高度冗余却更噪；② 量能门槛把候选池
从 278→107、67→25，主动剔除了许多“先价涨后量跟”的有效趋势；③ 冗余过滤降低分散度。
所以**核心策略不加量能门槛**，通达信里仅把它画成小三角供你肉眼参考。

---

## 5. 当前 Regime 状态（信号日 2026-07-31）

- 上证周线 **低于** 56 周均线（3928）→ **Regime = 空头**。
- 两版指标当前都会显示**淡红背景 + “空仓”**，不会出红箭头买点。
- 翻多触发位：**上证收盘 > 3928** 后，再按候选清单对应 4 仓执行。

---

## 6. 参数对照（与回测引擎一致）

- 动量窗口 `N_MOM = 52`（52 周）
- Regime 均线 `N_REG = 56`（上证 56 周）
- 量能窗口 `N_VOL = 26`（26 周 OBV / 量 MA）
- 估值分位近似：`HHV(C,260)*0.85`（回测用 260 周估值分位 ≥85）

---

*数据来源：日线=新浪 K 线；基本面=东方财富 DMSK 年报；指数=上证(sh000001) 周线。
回测为历史模拟，含成本与 regime 空仓，不代表未来收益。本报告/指标仅供参考，不构成个人投资建议。*
