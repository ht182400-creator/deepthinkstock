# 通达信公式 · 质量动量 + Regime 策略（K线标记版）

> 配套 Python 策略：`backtest_fund.py` 的 `score_mode="qscore"` + regime 过滤（500 只 CSI300/500 宇宙，全样本回测）。
> 本公式把策略的**技术信号层**搬到通达信 K 线图上，画出三类标记：
> - 🔴 **红背景** = 空仓区（指数跌破 13 月均线，全市场同步空仓）
> - 🔺 **红箭头↑** = 建仓信号
> - 🔻 **绿箭头↓** = 卖出信号

---

## ⚠️ 必须先读的前提（很重要）

1. **质量基本面门不在公式里。** ROE 连续 >20%、FCF/净利 ∈[80%,300%]、负债率 <30% 这些因子，标准通达信拿不到"时点历史值"（只能用 `FINANCE()` 取最新值，有前视偏差）。**必须先用离线清单（`buy_list.html` 的 5 只建仓股 / 141 只观察池）筛好标的，再在这些股票上加载本公式。** 在不合格股票上加载，箭头会误导。
2. **箭头发条件是"条件翻转"，不是"每月调仓"。** 策略本身是**月末调仓**。请以**每月最后一根月K收盘**的信号为准操作，箭头只是可视化辅助，不必每天追。
3. **估值分位用价格位置代理。** 真实策略用 PE/PB 历史分位；通达信里用 `HHV/LLV` 价格位置近似（≥85% 视为泡沫），会有一致性偏差。
4. **13 月均线用 270 交易日近似。** 如需精确，改用月线周期 + `MA(C,13)#MONTH`。
5. **回测结论来自 Python 全样本，不是通达信。** 通达信专家系统回测可做粗验，但与本策略的 Python 结果口径不同（含分红、成本、时点基本面均缺失）。

---

## 公式一：主图叠加指标（推荐，单一指标完成三标记）

打开通达信 → `Ctrl+F` → 技术指标公式 → 其他类型 → 新建
- 名称：`质量动量V1`
- 公式类型：**主图叠加**
- 画线方法：副图也可（但红背景+箭头要主图叠加才贴在K线上）

```
{=== 质量动量策略 V1（通达信·主图叠加）===}
{标记：建仓↑红箭头 / 卖出↓绿箭头 / 空仓红背景}
{前提：仅在已通过基本面质量筛选的标的上使用}

MOMN:=250;     {动量窗口≈12个月(交易日)}
MAW:=270;      {regime：指数13个月≈270交易日}
TRENN:=60;     {趋势MA≈3个月，破位出场}
VALN:=250;     {估值分位窗口}
CAP:=300;      {单价上限(元)}

{—— 市场regime（上证指数） ——}
指数:=INDEXC;
REG持仓:=指数>MA(指数,MAW);
REG空仓:=NOT(REG持仓);

{—— 动量（12个月） ——}
MOM:=C/REF(C,MOMN)-1;

{—— 估值分位（价格位置代理） ——}
PCTL:=(C-LLV(C,VALN))/(HHV(C,VALN)-LLV(C,VALN));

{—— 趋势MA ——}
TRE:=MA(C,TRENN);

{—— 信号定义 ——}
建仓:=REG持仓 AND MOM>0 AND C<CAP AND PCTL<0.85 AND C>TRE;
平仓:=REG空仓 OR C<TRE OR PCTL>=0.85 OR C>=CAP OR MOM<=-0.40;

{—— 绘图 ——}
DRAWGBK(REG空仓, COLORRGB(60,22,22));               {空仓：红背景}
DRAWICON(建仓 AND NOT(REF(建仓,1)), LOW*0.97, 1);     {建仓：红箭头↑}
DRAWICON(平仓 AND NOT(REF(平仓,1)), HIGH*1.03, 2);    {卖出：绿箭头↓}

持仓状态:IF(REG持仓,1,0),COLORWHITE,LINETHICK1;
0,COLORGRAY,DOTLINE;
```

> 说明：`DRAWICON` 第 3 参数图标号——`1` 通常为红箭头(向上)，`2` 为绿箭头(向下)。若你的通达信版本图标样式不同，改这两个数即可（如改用 `4`/`5` 等）。`持仓状态` 那行是可选项，嫌挤可删。

---

## 公式二：专家系统公式（可选，用于通达信自带回测）

若想用通达信内置"专家系统"做一遍粗略回测、自动出 B/S 信号，再建一个：
- 名称：`质量动量ES`
- 公式类型：**专家系统公式**

```
{=== 质量动量策略（通达信·专家系统）===}
MOMN:=250; MAW:=270; TRENN:=60; VALN:=250; CAP:=300;
指数:=INDEXC;
REG持仓:=指数>MA(指数,MAW);
REG空仓:=NOT(REG持仓);
MOM:=C/REF(C,MOMN)-1;
PCTL:=(C-LLV(C,VALN))/(HHV(C,VALN)-LLV(C,VALN));
TRE:=MA(C,TRENN);
建仓:=REG持仓 AND MOM>0 AND C<CAP AND PCTL<0.85 AND C>TRE;
平仓:=REG空仓 OR C<TRE OR PCTL>=0.85 OR C>=CAP OR MOM<=-0.40;
ENTERLONG:建仓;
EXITLONG:平仓;
```

> 专家系统会自动在 K 线上画 B（买入）/S（卖出）文字信号，并支持系统内回测。但同样**不含质量门、不含分红、不含成本**，结果仅供粗验。

---

## 安装与使用步骤

1. `Ctrl+F` 打开公式管理器 → 技术指标公式 → 其他类型 → 新建 → 名称 `质量动量V1`、类型选"主图叠加" → 粘贴【公式一】→ 确定。
2. （可选）再新建"专家系统公式" `质量动量ES`，粘贴【公式二】→ 确定。
3. 打开**已筛过的标的**（如 洛阳钼业 / 中国巨石 / 紫金矿业 / 亚钾国际 / 宝丰能源）的 K 线。
4. 输入 `质量动量V1` 回车加载：红背景=空仓区，红箭头=建仓，绿箭头=卖出。
5. **操作纪律**：只在每月最后一根月K出现红箭头且仍处于红背景之外（即 regime 持仓）时，于次月初建仓；绿箭头或进入红背景时平仓。当前（2026-07-31）regime 刚翻持仓，按策略 8 月初启动建仓。

---

## 参数调校（如需对齐 Python 默认值）

| 参数 | 公式值 | 含义 | Python 对应 |
|------|--------|------|-------------|
| `MOMN` | 250 | 动量窗口(交易日) | `mom_window=12` 月 |
| `MAW` | 270 | 指数 regime MA(交易日) | 指数 13 月均线 |
| `TRENN` | 60 | 趋势/破位 MA | 价格 3 月均线（趋势确认+破位出场） |
| `VALN` | 250 | 估值分位窗口 | 估值泡沫分位（价格代理） |
| `CAP` | 300 | 单价上限 | `price<=300` |
| `PCTL>=0.85` | 0.85 | 泡沫阈值 | `bubble_pctl=85` |
| `MOM<=-0.40` | -0.40 | 动量崩溃阈值 | `momentum<=-40%` |

---

## 诚实边界总结

- ✅ 公式忠实还原：**动量 + 指数 regime 过滤 + 趋势破位 + 估值泡沫 + 单价上限** 这一技术信号层，三类标记清晰。
- ❌ 公式**不能**还原：质量基本面门（ROE/FCF/负债率时点筛选）、含分红再投、交易成本、100 股手数约束。这些由离线 Python 清单（`buy_list.html`）负责。
- 因此：**通达信图 = 执行层可视化；Python 清单 = 选股层。** 两者配合才是完整策略。

---

*本报告仅供参考，不构成个人投资建议。*
